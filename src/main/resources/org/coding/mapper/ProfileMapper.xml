<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.coding.mapper.ProfileMapper">

<insert id="insert">

	insert into profile(uuid, uploadpath, filename, image, id)
	values(#{uuid}, #{uploadPath}, #{fileName}, #{image}, #{id})

</insert>

</mapper>